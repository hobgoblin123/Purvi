# 10-06-2026

## Sync

**Private by default** — All new GitHub repos start private; going public is an explicit later decision. Existing repos audited by user — all already private.

**Sync boundary resolved** (carried from 09-06) — The automated Purvi step in update-context only *flags* candidate patterns; it never writes rules. The dedicated product sync reviews flagged candidates and promotes or drops them. Flags queue in `flagged.md` in the Purvi repo: update-context appends (with approval), purvi-sync surfaces them at start and clears them once decided. No rule-writing outside a sync — no loop.

## Sync (evening, post-PWA rollout)

**Cookieless fetches are part of the logged-out surface** — promoted to Janmam. Candidate came straight from the session (PWA rollout applied the iris login-CSS lesson proactively across gallery/finance/cycle); downside check done before promoting — guideline is scoped to static app-identity assets only, token guards for any cookieless *data*.

**Consistency is a feature** — promoted to Janmam at Nithin's direction, two scales made explicit: across projects (replicate the house pattern verbatim, diverge only by decision) and within an app (widget vs dashboard must share one rule and one format — the iris ordering/ticker example).

**Watchdog observation:** flag queue was empty despite the full iris build day + PWA session since the morning sync — the flagging mechanism only existed from mid-day, so an empty queue from before then means "didn't run," not "clean." Future syncs: the watchdog count in purvi-sync covers this.

## To-do's

- [x] ~~Resolve automated vs dedicated sync boundary~~
- [x] ~~Implement flagged.md mechanism (update-context + purvi-sync edits)~~ — done same session, right after the sync
- (evening sync: no new to-do's)

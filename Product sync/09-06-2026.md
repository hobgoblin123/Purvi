# 09-06-2026

## Sync — 00:30

Continued from yesterday's sync. Focused on hardening Purvi's integration into the session lifecycle.

### Discussion

**Adversarial review of Purvi integration** — Listed 10 failure modes for the automated sync in update-context. User addressed each one:
- No auto-writes: everything needs approval before writing to Janmam/checklist/sync notes
- No noise entries: if nothing worth noting, stay silent
- Optional when context is tight
- Subtle failures flagged manually by user for now
- Pull before push to handle stale local copies
- Clarify before closing: no silent unknowns

**Ground truths formalized as `.env` pattern** — Device-specific operational facts (auth, paths, services) go in a local `ground-truths.md` file. Never committed, never backed up. Loaded at session start by gain-context. Purvi documents the principle; the local file holds the facts.

**purvi-sync skill created** — Dedicated skill for "Assembly dispersed" product syncs. User drives, AI captures and clarifies. All writes require approval.

**Skill diff check** — gain-context now compares live skills against Purvi dependency snapshots at configurable frequency (daily/weekly/monthly/yearly/never). Set to weekly for this machine.

**Git worktrees investigated** — Not a fit for current setup (hardcoded systemd paths, 288MB duplication, secrets don't carry, single-branch workflow).

**"Don't assume, always check"** — AI assumed worktrees had no downsides without inspecting the actual repos. User caught it. Reinforces the ground truth principle — never answer "will X work for us?" from general knowledge.

---

## To-do's

- [x] ~~Document GitHub auth in local ground truths~~
- [x] ~~Rewrite update-context Step 6~~
- [x] ~~Create purvi-sync skill~~
- [x] ~~Formalize ground truths as .env pattern~~
- [x] ~~Add skill diff check to gain-context~~
- [x] ~~Resolve automated vs dedicated sync boundary~~ (resolved 10-06 by "Flag in passing, decide in person" + the flagged.md loop; confirmed working 12-06 when 4 flags flowed through the pipeline end-to-end)

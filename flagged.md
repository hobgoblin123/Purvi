# Flagged Patterns

Candidate patterns flagged by automated session wrap-ups (update-context Step 6). This is a queue, not a rulebook — nothing here is a rule yet.

At each dedicated product sync, every flag below is reviewed and either **promoted** (to Janmam and/or checklist) or **dropped**, then removed from this file. The file should be empty after every sync.

Format: `- [ ] DD-MM-YYYY — <candidate pattern in one sentence> (context: <which session/project it came from>)`

## Queue

- [ ] 11-06-2026 — An "already handled, stay silent" check must scope its lookback window to the specific obligation it guards — "any activity since the last checkpoint" silences the wrong things (context: iris dose reminders; a late morning dose would have muted the evening slot, fixed with per-slot cyclic-midpoint windows)
- [ ] 12-06-2026 — When a new feature idea conflicts with the user's own established design rule, surface the tension explicitly before building — the conscious override or redesign beats silently following either (context: finance_sheet; "no dashboard info icons" rule vs miscategorisation badge idea → entry-time warning instead, cleaner than both)
- [ ] 12-06-2026 — Every aggregation over a two-sided ledger (income/expenses) must take an explicit stance on both sides — an expenses-only sum is a latent bug until the first credit appears; when a new entry kind shows up in real data, audit every calculation path that consumes it (context: finance_sheet; card exposure ignored refund income while Salary Spend netted it, Net Position sat ₹1,700 low)
